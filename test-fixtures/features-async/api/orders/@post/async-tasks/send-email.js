/**
 * Async Task: Send email (runs in background)
 */
module.exports = async function sendEmail(context) {
  // This runs in the background after response is sent
  // In tests, we just verify the response was immediate
}
